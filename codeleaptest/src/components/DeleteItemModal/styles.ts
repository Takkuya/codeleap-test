import styled from "styled-components"

export const DeleteItemModalContainer = styled.div`
  #deleteModalWrapper {
    display: flex;
    flex-direction: column;
    gap: 1rem;

    max-width: 450px;
    width: 100vw;
    padding: 1rem 2rem;
  }
`
